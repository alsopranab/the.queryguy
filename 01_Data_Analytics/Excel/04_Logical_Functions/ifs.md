# IFS

## What it does
Simplifies multiple IF conditions into a single function.

## Syntax
`=IFS(condition1, result1, condition2, result2, condition3, result3)`

## Example
`=IFS(A2>=100000,"High", A2>=50000,"Medium", A2<50000,"Low")`

## Advantage
Cleaner and easier to maintain than nested IF.
