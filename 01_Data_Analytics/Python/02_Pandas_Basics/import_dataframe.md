# Importing Pandas & Creating DataFrames

## What it does
Pandas is used to load, manipulate, and analyze structured data.

## Import Pandas
`import pandas as pd`

## Create DataFrame from dictionary
`data = {"Month": ["Jan","Feb"], "Sales": [1000,1500]}`  
`df = pd.DataFrame(data)`

## Interview Tip
Most analysis starts with a DataFrame.
