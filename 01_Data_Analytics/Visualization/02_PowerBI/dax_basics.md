# DAX Basics

## What is DAX
Data Analysis Expressions used for calculations.

## Common Functions
- `SUM()`
- `COUNT()`
- `CALCULATE()`
- `FILTER()`

## Example Measure
`Total Orders = COUNT(Orders[Order_ID])`

## Interview Tip
DAX is evaluated based on filter context.
