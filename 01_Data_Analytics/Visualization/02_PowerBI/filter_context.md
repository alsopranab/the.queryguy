# Filter Context

## What it means
Filters applied by slicers, visuals, or DAX expressions.

## Example
`CALCULATE(SUM(Sales[Revenue]), Sales[Region]="North")`

## Interview Line
Filter context controls what data DAX sees.
