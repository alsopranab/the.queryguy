# Measures vs Calculated Columns

## Measures
Calculated at report level  
Example: `Total Sales = SUM(Sales[Revenue])`

## Calculated Columns
Calculated row by row  
Example: `Profit = Sales[Revenue] - Sales[Cost]`

## Interview Line
Use measures for aggregation, columns for row-level logic.
